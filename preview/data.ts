import {starterExercises,type Snapshot} from '../lib/model';
export function createPreviewData():Snapshot {const now=Date.now();return {exercises:starterExercises.map(v=>({id:'ex-'+v.key,name:v.base})),variants:starterExercises.map(v=>({...v,id:v.key,exerciseId:'ex-'+v.key})),sessions:[{id:'known',name:'Known history',startedAt:0,endedAt:null,status:'seed',notes:'User-supplied history. Date, side, and RIR were not supplied.',plan:['extension'],rules:{}},{id:'today',name:'Leg day',startedAt:now,endedAt:null,status:'active',notes:'',plan:starterExercises.slice(0,5).map(v=>v.key),rules:{}}],sets:[{id:'known-set',variantId:'extension',sessionId:'known',createdAt:0,weight:95,reps:10,rir:null,side:'unknown',type:'working',painLocation:'',painSeverity:0,note:'User-supplied performance; side and date unknown.',suggestedWeight:null,suggestedReps:null}],messages:[],progressions:[]};
}
